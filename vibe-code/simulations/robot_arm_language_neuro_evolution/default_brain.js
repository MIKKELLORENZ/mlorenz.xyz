/* default_brain.js — placeholder. No champion has been baked into this checkout yet.
 *
 * Run a training session and bake the result:
 *   node train.js --gens 400 --pop 60
 *   node bake_brain.js
 *
 * Until then the page's "watch the trained champion" option is disabled and
 * only "evolve from scratch" is available. */
"use strict";
const DEFAULT_BRAIN = null;
if (typeof module !== "undefined") module.exports = { DEFAULT_BRAIN };
